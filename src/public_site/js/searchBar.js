const searchBar = document.getElementById("country-search-bar");

const showResults = () => {
  const countryNames = document.getElementsByClassName("country-name");


};

searchBar.addEventListener("input", showResults);
